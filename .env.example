# Get from https://kie.ai → API keys
KIE_API_KEY=

# Set during `npx -y freebeat-mcp config` — usually picked up by the MCP itself,
# but keep it here too so scripts can read it if needed.
FREEBEAT_API_KEY=
